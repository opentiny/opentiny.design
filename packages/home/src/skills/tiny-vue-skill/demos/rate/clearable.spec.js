import{test,expect}from'@playwright/test';test.describe('Rate Clearable 功能',()=>{test('点击已选中的星星可以清零',async({page})=>{page.on('pageerror',exception=>expect(exception).toBeNull());await page.goto('rate#clearable');const icon=page.locator('.tiny-rate__star > .tiny-svg');const text=page.locator('.tiny-rate .tiny-rate__text');await page.waitForTimeout(100);const initialText=await text.textContent();expect(['一般','中等','普通'].includes(initialText)).toBeTruthy();await icon.nth(2).click();await page.waitForTimeout(100);const clearedText=await text.textContent();expect(clearedText===''||clearedText.includes('未评分')).toBeTruthy();});test('未设置 clearable 时点击已选星星不会清零',async({page})=>{page.on('pageerror',exception=>expect(exception).toBeNull());await page.goto('rate#basic-usage');const icon=page.locator('.tiny-rate__star > .tiny-svg');const text=page.locator('.tiny-rate .tiny-rate__text');await page.waitForTimeout(100);const textBefore=await text.textContent();await icon.nth(1).click();await page.waitForTimeout(100);const textAfter=await text.textContent();expect(textAfter).toBe(textBefore);});test('clearable 模式下点击其他星星正常选择',async({page})=>{page.on('pageerror',exception=>expect(exception).toBeNull());await page.goto('rate#clearable');const icon=page.locator('.tiny-rate__star > .tiny-svg');const text=page.locator('.tiny-rate .tiny-rate__text');await page.waitForTimeout(100);await icon.nth(0).click();await page.waitForTimeout(100);let currentText=await text.textContent();expect(['很差','非常差','差'].some(t=>currentText.includes(t))).toBeTruthy();await icon.nth(0).click();await page.waitForTimeout(100);currentText=await text.textContent();expect(currentText===''||currentText.includes('未评分')).toBeTruthy();await icon.nth(3).click();await page.waitForTimeout(100);currentText=await text.textContent();expect(['好','良好','不错'].some(t=>currentText.includes(t))).toBeTruthy();});});